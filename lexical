// LEXICAL Correctness
#include <stdio.h>
int minimum(int x, int y) {
        // This will compare 2 numbers
        if (x > y)
            return x;
        else {
            return y;
        }
    }
